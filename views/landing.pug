doctype html
html(lang='en')
  include common/head.pug
  +head("Starter MVP")
  body
   header
    include common/navbar.pug
    +navbar(user)
    .container
      .row-align-items-center
        .col-md-6.order-md-1.text-center.text-md-left.pr-md-5
          h1.mb-3 New product announcment!
          p.lead A sales lead collecting web page using Node.JS and Express.JS. Pug is used as a templating engine.
          p.lead.mb-4 Sample page with basic CRUD operations with BootStrap and JQuery.
          form(action="/", method="POST")
            .input-group.mb-3
              input(name="lead_email", type="email", placeholder="Enter your email address").form-control
              .input-group-append
                button(type="submit").btn.btn-primary Get Started
   include common/footer.pug
   +footer()
   //- script(src='https://code.jquery.com/jquery-3.4.1.min.js')
